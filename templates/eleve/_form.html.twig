{# {{ form_start(form) }}
{{ form_widget(form) }}

<button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
 #}

{{ form_start(form) }}

<div class="mb-3 mt-3">
	{{ form_row(form.nom_eleve) }}
</div>
<div class="mb-3 mt-3">
	{{ form_row(form.date_naissance) }}
</div>


<button class="btn btn-primary">{{ button_label|default('Sauver') }}</button>
{{ form_end(form) }}
